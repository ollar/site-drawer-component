{{yield (hash
            aside=(component this.asideComponent toggleDrawer=this.toggleDrawer openDrawer=this.openDrawer closeDrawer=this.closeDrawer opened=this.opened)
            overlay=(component this.overlayComponent toggleDrawer=this.toggleDrawer)
            trigger=(component this.triggerComponent toggleDrawer=this.toggleDrawer)

            opened=this.opened
            toggleDrawer=this.toggleDrawer
            openDrawer=this.openDrawer
            closeDrawer=this.closeDrawer
        )
}}